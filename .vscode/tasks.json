{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pyinstaller: Compile",
            "type": "shell",
            "command": "pyinstaller --noconfirm --onedir --console --name \"warnbot-discordpy\" --add-data \"${workspaceFolder}\\cogs\\;cogs\" --add-data \"${workspaceFolder}\\data;data\"  \"${workspaceFolder}\\warnbot.py\"",
                "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}